// Package config loads the chriscast TOML configuration file, resolves
// XDG base directories for cache and config locations, and supplies
// default values when fields are missing or the file is absent.
package config
