// Package ui renders the chriscast overlay window using GTK3. The GTK
// implementation is only compiled when the gtk build tag is set; other
// files in the package provide a stub so the rest of the project builds
// without GTK headers installed.
package ui
